language: java
branches:
  only:
    - develop
    - /^release-[0-999].[0-999]$/
    - master
notifications:
  email:
    recipients:
      - andres.grisalesg@udea.edu.co
before_install:
  - chmod +x gradlew
script:
  - ./gradlew clean build